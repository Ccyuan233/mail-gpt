# 邮件机器人可靠性检查 · 2026-09-07

本次复现并修复 4 个边界问题，新增 5 项回归测试。共 47 项离线测试通过，覆盖真实处理流程、临时 SQLite 和模拟邮件/CLI；测试不向真实邮箱发送测试消息。

## 已修复

| 触发情况 | 旧行为 | 修复后 |
| --- | --- | --- |
| 旧请求投递不确定或生成中断后，发送 `[GPT:NEW]` | 新话题仍等待旧请求，无法独立恢复 | 为新话题创建独立内部会话，旧请求保留待核对，避免盲目重发 |
| 同一 Gmail 分组内新建话题，再回复旧答案 | 新话题覆盖同一会话的关联，旧回复可能进入新上下文 | 保留旧会话；优先按明确的回复引用选择上下文，再回退到 Gmail 分组 |
| 回答已发出，但 IMAP 标记已读失败 | 整轮轮询终止，后面的请求无法处理 | 单独记录标记失败，继续处理本轮已下载请求；不重发已完成回答 |
| 查询状态期间，后台处理完成一封邮件 | 仍用扫描前的数据库记录，误报尚未处理 | 邮箱读取完成后，再取短事务数据库快照进行核对 |

原始复现命令：`python -m unittest tests.test_audit -v`，修复前 4 项测试失败；修复后增加重启、旧会话保留与去重验证，全部通过。

## 实际运行核对

- 修复前真实邮箱与数据库均有 10 封请求，均已发送，无待核对或生成中的请求。
- 已在数据库写锁保护下确认没有生成/发送中的任务，再停止旧进程并备份数据库，避免切换时中断新请求。
- 新后台于北京时间 22:53 启动，首轮 `checked=10, sent=0`，确认既有消息未被重复发送。
- CLI 兼容性检查通过。本次没有改变模型或思考强度，也没有再调用真实模型做压力测试。
- 状态查询是时间点快照；检查不等于保证未来没有故障，也不证明 QQ 客户端实际入箱。

## 建议后续按优先级增加

1. **开机自启、异常退出恢复与故障提醒。** 当前电脑重启后需要手动运行。可增加 Windows 登录启动、进程异常重启和异常通知；不应自动重试投递结果不确定的回答。
2. **增量收信。** 当前每轮重新下载扫描起始日期后的候选邮件，数量增长会延长等待。本次 10 封候选的新版首轮扫描约 23 秒，另有 30 秒轮询间隔。建议持久化 UIDVALIDITY 与已核对 UID，保留待处理队列及定期全量核对，避免再次依赖已读标记而漏信。
3. **思考强度选项。** 增加可校验的“中／高”配置，状态中显示实际模型与强度，并按模型能力检查。当前继续使用默认中等强度；提高强度可能增加耗时和额度消耗。
4. **清晰的处理反馈。** 可增加中文错误说明、排队状态和筛选原因，让用户区分“排队中”“需人工核对”和“正文/附件超限”。当前大附件和部分无效邮件仍按既有规则忽略。
5. **可判定未发送时的有限重试。** 区分连接/登录失败与 SMTP 已提交后的断线。只有能确认邮件尚未提交时才退避重试；当前所有发送异常都会保守进入人工核对。

以上功能是后续建议，本次未启用自启、自动提醒、额外重试或修改思考强度。

## 后续更新：自启与故障提醒

用户随后要求实现第 1 项。已增加 Windows 登录后自启、每 5 分钟补查、进程退出后的退避重启、持久暂停、故障合并和恢复通知。通知通道和暂停/恢复已在本机实际验证，完整测试增加到 64 项；详见 README 的自启操作说明。第 2 至 5 项中的其他增强仍未启用，思考强度保持原设置。

## 2026-09-08：通知下的提问被主题筛选漏掉

一封直接回复运行提醒的授权来信没有 `[GPT]` 标签，因此旧版 IMAP 搜索根本没有读取它，状态查询同样漏查。修复为扫描白名单发件人的全部主题，将未知主题显示为 `filtered`；只有引用本机实际通知编号、且通过发件人认证的通知回复会按提问处理。通知编号持久保存，不随最近通知日志裁剪而丢失。回答主题归一到 `[GPT]`，普通邮件和伪造通知引用不会自动触发。真实来信离线回放通过，且没有启用联网搜索或其他模型工具。

## 2026-09-08：按用户要求增加实时联网搜索

增加 `CODEX_WEB_SEARCH=live|disabled`（默认 live），同步设置 CLI 搜索、开发者指令及 JSONL 工具事件检查。搜索回答要求带完整来源 URL、说明证据不足和同名主体，网页内容不具有指令权限。保持只读沙箱与其余工具限制。默认生成时限从 240 秒调到 480 秒，工作进程心跳显示实际加载的搜索模式和时限。

真实测试发现仅改 `web_search=live` 时，CLI 0.153.4 的 `tools.web__run` 被 `code-mode host is disabled` 拦截，最终答案明确表示无法检索，搜索事件为 0。只开启工具调用宿主 `code_mode_host` 后，同一问题实际产生 5 次搜索/读取调用并返回来源网址，其他能力开关保持关闭。离线模拟无法发现这个 CLI 内部依赖，因此保留真实事件验证作为部署依据。

全部 77 项离线测试通过，包括搜索开关、新建/续接权限、关闭模式拒绝搜索、其他工具事件拒绝、网址经邮件编码后的保留、去重，以及 480 秒时限下的故障提醒等待。真实回答只把可读取来源作为依据，对未读取到的公司官网及 PDF 明确说明限制。测试会话及原始检索资料仅保存在忽略于 Git 的本机 diagnostics 目录。

正式配置下恢复同一个真实测试会话，模型正确回答上一封查询的公司简称，session ID 保持一致。安全暂停旧工作进程后，本机配置启用 live 和 480 秒时限；查询结果经原有通知队列发给已授权的通知收件人，保留通知的去重和不确定投递保护。
